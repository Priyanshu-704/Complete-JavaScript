let x = myFunction(4, 3);// call the function
console.log(x);
//define a function using "function" keyword
function myFunction(a, b) { // "a" and "b" are arguments which are passed in the function as parameter
  return a * b;
}
