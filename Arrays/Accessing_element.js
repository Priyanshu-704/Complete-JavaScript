let array = new Array(10,20,13);
console.log(array[0]);//First element in the array can be accessed using the index number "zero".

//We can access the last array element using [array.length – 1] index number.
console.log(array[array.length-1]);

console.log();//add a break line

let arr = ["ram", 12, null];
console.log(arr[0]);
console.log(arr[1]);
console.log(arr[2]);
