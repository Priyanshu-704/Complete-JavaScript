let value = 5;
let isItBiggerThanTen = (value > 10) ? "greater than 10" : "not greater than 10";
//Ternary operator is a shorthand of the "if- else" statement.
//Syntax
//condition ? value_if_true : value_if_false
console.log(isItBiggerThanTen);