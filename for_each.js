//The forEach() method in JavaScript is used to execute a provided function once for each array element. It is a convenient way to iterate over arrays without needing to manage the loop counter manually.
const fruits = ['apple', 'banana', 'cherry', 'date'];

// Using forEach to log each fruit to the console
fruits.forEach(function(fruit) {
    console.log(fruit);
});